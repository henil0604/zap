{
  "name": "@zap-project/backend",
  "version": "1.0.50",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun run --watch src/index.ts",
    "build": "bun build --compile --minify --minify-syntax --target bun --outfile build/server ./src/index.ts",
    "start": "bun run build/server"
  },
  "dependencies": {
    "@elysiajs/cookie": "^0.8.0",
    "@elysiajs/swagger": "^1.1.1",
    "@lucia-auth/adapter-prisma": "^4.0.1",
    "dotenv": "^16.4.5",
    "elysia": "latest",
    "lucia": "^3.2.0"
  },
  "devDependencies": {
    "bun-types": "latest"
  },
  "module": "src/index.js"
}
